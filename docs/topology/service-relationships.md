# Service Relationships

This ecosystem contains code repositories, protocol repositories, manifest
repositories, and managed project repositories. The relationships are not all
service-to-service in the narrow sense.

Important relationship categories:

- owns canonical schema
- consumes topology
- validates policy
- invokes runtime
- produces artifacts
- hosts local deployment
