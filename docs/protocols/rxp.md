# RxP

RxP owns runtime invocation and return semantics.

## Main objects

- `RuntimeInvocation`
- `RuntimeResult`
- transport and runtime metadata

## Why it exists

It separates runtime mechanics from orchestration semantics and keeps runtime
boundaries explicit.
