# Runtime Governance

Runtime governance keeps runtime invocation and execution behavior bounded.

It should cover:

- runtime selection
- runtime binding policy
- environment assumptions
- invocation safety
- artifact retention expectations
